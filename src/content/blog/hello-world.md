---
title: 'Hello, world'
description: 'A first post — placeholder text, here to prove that plain Markdown posts work too.'
pubDate: 2026-08-15
tags: ['misc']
---

TODO — replace this with your actual first post.

This file is plain `.md` rather than `.mdx`, which is the right choice when a
post is only prose, code and maths. You still get everything except components:

```rust
fn main() {
    println!("Hello, world!");
}
```

Maths works here as well — the sum $\sum_{k=1}^{n} k = \frac{n(n+1)}{2}$ renders
exactly as it would in an MDX post.

To delete this post, delete this file.
